select * from users where uid = "dfgrhtjykl"

create table users (id integer primary key not null, isFirst integer, uid text, userName text);

create table (?) (id integer primary key not null, pdc text, pathh text, byy text);
[uid]

insert into (?) (pdc, pathh, byy) values (?,?,?)
[uid+"nice", postDoc, path, by]

select * from (?) order by id desc
